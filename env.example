# Copy to .env and fill in
WC_DB_PATH=./data/weight.db
WC_API_HOST=127.0.0.1
WC_API_PORT=8765
WC_WEB_DIST=./web/dist

# Oura personal access token: https://cloud.ouraring.com/personal-access-tokens
OURA_TOKEN=

# Web Push (VAPID) — generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com

# Nightly check-in local time (24h)
CHECKIN_HOUR=23
CHECKIN_MINUTE=0

# Program start date (YYYY-MM-DD) — anchors cumulative deficit
PROGRAM_START=2026-07-27

# Estimated maintenance kcal — recalibrated automatically after 14 days of data
MAINTENANCE_KCAL=2500

# Basal metabolic rate — used only for days without Oura data, to still show an "Out" number
BMR_KCAL=1700

# Tuya Smart / Smart Life scale integration
# Endpoints:
#   Central Europe: https://openapi.tuyaeu.com  ← Sweden/Nordic accounts
#   Western Europe: https://openapi-weaz.tuyaeu.com
#   Western America: https://openapi.tuyaus.com
#   India:          https://openapi.tuyain.com
TUYA_ENDPOINT=https://openapi.tuyaeu.com
TUYA_ACCESS_ID=
TUYA_ACCESS_SECRET=
TUYA_DEVICE_ID=
# Owner user ID of the linked app account (find via the debug script)
TUYA_UID=

# Garmin Connect (community garminconnect lib — uses your normal login).
# First login handles MFA via a one-shot script; tokens then cache in this dir.
GARMIN_EMAIL=
GARMIN_PASSWORD=
GARMIN_TOKENSTORE=~/.garminconnect

# Discord bot (leave DISCORD_TOKEN empty to disable the bot)
DISCORD_TOKEN=
# Your Discord user ID (right-click your name in Discord → Copy User ID; requires
# Developer Mode: Settings → Advanced → Developer Mode). The bot ignores anyone else.
DISCORD_USER_ID=
# Optional: register slash commands to this guild (server) only for fast propagation.
# Global commands take up to 1 hour. Right-click server icon → Copy Server ID.
DISCORD_GUILD_ID=

# Local LLM for meal estimation (Ollama). Install: https://ollama.com
OLLAMA_URL=http://localhost:11434
OLLAMA_URLS=http://homeai.local:11434,http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# Cloud fallback if Ollama unavailable (leave OPENAI_API_KEY empty to disable)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1
